<?xml version="1.0" encoding="UTF-8"?>
<mapper>
    <sql id="duplicate">SELECT 'second'</sql>
    <sql id="conditional">
        SELECT 1
        <if test="enabled = TRUE"> WHERE id = #{id}<else> WHERE id IS NULL</else></if>
    </sql>
</mapper>
